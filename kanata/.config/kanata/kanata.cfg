(defcfg
  process-unmapped-keys yes
  )

(defsrc
  caps
  )

(defvar
  tap-timeout 150
  hold-timeout 150
  tap-dance-timeout 150

  tt $tap-timeout
  ht $hold-timeout
  tdt $tap-dance-timeout
  )

(defalias
  caps_mod (tap-hold $tt $ht esc ctl)
  )

(deflayer base-layer
  @caps_mod
  )
