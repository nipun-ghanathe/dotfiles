(defcfg
  process-unmapped-keys yes
  )

(defsrc
  caps ins z /
  )

(defalias
  caps_mod (tap-hold 150 150 esc ctl)
  ctl_sft (multi ctl sft)
  z_mod (tap-hold 100 180 z @ctl_sft)
  slash_mod (tap-hold 100 180 / @ctl_sft)
  )

(deflayer base-layer
  @caps_mod caps
  @z_mod @slash_mod
  )
